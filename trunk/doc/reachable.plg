
/** All-Pairs All Paths */

materialize(neighbor,2,infinity,infinity,0).
materialize(reachable,2,infinity,infinity,1).

rule r1 reachable@X(X,Y) :- neighbor@X(X,Y).

rule r2 reachable@X(X,Y) :- neighbor@Z(Z,X), 
	                  reachable@Z(Z,Y),
	                  ne(X,Y). 

