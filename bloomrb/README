To install:
	% gem build bloom.gemspec
	% sudo gem install bloom-0.0.1.gem

A "one-shot" shortest paths example:
  % ruby examples/paths.rb

To run a "ping-pong" example, see instructions in examples/pingpong.rb