import java.lang.Integer;
import java.lang.Float;
import p2.types.table.Index;

define(t1, infinity, infinity, keys(1), 
 {string, string, string});

define(event, {String, Float, Float});

t1("test", "this", "fact");

rule1 event(Id, 1.0, 2.0) :-  periodic(Id, 2L);

rule2 t1(A, B, C) :- event(A, X, Y), 
                     Y in [1.0, 5.0],
                     B := X.toString(),
                     C := Y.toString();