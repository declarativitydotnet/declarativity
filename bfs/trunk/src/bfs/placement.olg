program bfs;

watch(migration_choices, ae);
public
migration_choices(Master, DataNode, ChunkId) :-
    bfs_heartbeat::newReplica(Master, ChunkId, _, _, _),
    bfs_heartbeat::dataNodes(Master, DataNode, _),
    notin bfs_heartbeat::chunks(Master, DataNode, ChunkId, _);
