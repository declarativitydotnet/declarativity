set terminal postscript eps "Times-Roman" 26
set output "Write_Contention_F_Comparision.ps"
set xlabel "f (Faults tolerated)"
set ylabel "Fraction of contending writes"
unset key
set yrange [0:1]
set label "HQ-S < PBFT < HQ < Q/U" at 3, 0.8
set label "HQ-S < HQ < Q/U < PBFT" at 3, 0.4
set label "HQ < Q/U < HQ-S < PBFT" at 3, 0.1
plot "contention_boundary_pbft_hq.dat" with linespoints, 0.57, 0.142

