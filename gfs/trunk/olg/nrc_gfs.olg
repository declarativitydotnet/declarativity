program gfs;

// Request ID, Filename
define(cat_request,{Integer,String});

// Request ID, File contents
define(cat_response,{Integer,String});

// File name, contents
define(file,{String,String});

file("Foo", "Bar");
file("Bar", "Baz");

//master(A,B);

//sendRequest(@B,Id,File,A) :- master(@A,B), request(Id,File,@A);
