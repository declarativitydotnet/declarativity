program knn;

import java.lang.Double;
import java.lang.Integer;
import net.sf.javaml.clustering.mcl.Vectors;
//import java.lang.ValueList;

//rowid,row vector

define(hunk,{String});
define(skunk,{String});

watch(hunk,id);

// load("tests/matrix.txt",hunk,":");

define(foo, {java.lang.String});
watch(foo, s);
foo(X) :- periodic(Id, 2L), X := new Vectors().toString();


skunk(A) :- hunk(A);

//define(matrix,{Integer,

